---
import { EMAIL, PHONES } from "@/data/site-config"

// Shared phone + email display pair (no wrapper — multiple roots).
interface Props {
  emailClass?: string
}
const { emailClass = "" } = Astro.props
---

<span>{PHONES.main.formatted}</span>
<span class={emailClass}>{EMAIL.address}</span>
