---
// Plain <img> avatar — external URLs only (Unsplash), never astro:assets.
interface Props {
  src: string
  alt: string
  class?: string
}
const { src, alt, class: cls = "" } = Astro.props
---

<img
  src={src}
  alt={alt}
  width="96"
  height="96"
  loading="lazy"
  class:list={["h-24 w-24 rounded-full border-4 border-on-primary object-cover shadow-lg", cls]}
/>
